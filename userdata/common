#cloud-config-archive
- type: text/cloud-config
  content: |
    apt_upgrade: true
    packages:
    - puppet
    - git-core
    - ruby-json
- type: text/x-shellscript
  content: |
    #!/bin/sh
    mkdir -p /var/lib/drifter
    cd /var/lib/drifter
    git clone git://github.com/larsks/drifter-example-cloud-init.git config
    puppet apply --modulepath=config/puppet/modules config/puppet/node.pp

